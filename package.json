{
    "name": "entrepot",
    "version": "0.0.1",
    "description": "Store",
    "main": "dist/cjs/index.js",
    "module": "dist/esm/index.js",
    "typings": "dist/esm/index.d.ts",
    "scripts": {
        "// DEV SCRIPTS //": "-------------------------------------------------------",
        "start": "run-s watch:example",
        "watch": "npm-watch build",
        "build": "run-s clean build:esm build:cjs",
        "build:esm": "echo 'Building esm...' && tsc -p tsconfig.esm.json",
        "build:cjs": "echo 'Building cjs...' && tsc -p tsconfig.cjs.json",
        "watch:example": "parcel src/demo/index.html",
        "build:example": "parcel build src/demo/index.html",
        "// CODE QUALITY SCRIPTS //": "----------------------------------------------",
        "lint": "tslint -c tslint.json src/*.ts",
        "test": "jest --config jest.config.json",
        "test:watch": "jest --config jest.config.json --watch",
        "test:coverage": "jest --config jest.config.json --coverage",
        "// UTILS SCRIPTS //": "-----------------------------------------------------",
        "deploy:site": "gh-pages -d site -b master && run-s docs build:site ",
        "docs": "typedoc --mode modules --exclude \"**/*+(index|.spec|.e2e).ts\" --tsconfig tsconfig.esm.json --hideGenerator --readme README.md --out docs src",
        "clean": "echo 'Cleaning...' && rimraf dist && rimraf docs && rimraf site && rimraf coverage",
        "precommit": "run-s lint test build clean",
        "prepush": "run-s lint test"
    },
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "ignores": [
                "**/*.d.ts"
            ],
            "extensions": "ts",
            "quiet": false
        }
    },
    "devDependencies": {
        "@types/benchmark": "^1.0.31",
        "@types/jest": "^23.3.1",
        "@types/node": "^6.0.0",
        "benchmark": "^2.1.4",
        "gh-pages": "^1.1.0",
        "http-server": "^0.11.1",
        "husky": "^0.14.3",
        "jest": "^23.4.2",
        "node-sass": "^4.9.2",
        "npm-run-all": "^4.1.3",
        "npm-watch": "^0.3.0",
        "parcel": "^1.9.7",
        "rimraf": "^2.6.2",
        "ts-jest": "^23.1.2",
        "tslint": "^5.9.1",
        "typedoc": "^0.11.1",
        "typescript": "^2.9.2"
    },
    "engines": {
        "node": ">=6.1"
    },
    "author": "Filip Dabrowski",
    "repository": {
        "type": "git",
        "url": "git@github.com:thefill/entrepot.git"
    },
    "license": "MIT",
    "dependencies": {}
}
